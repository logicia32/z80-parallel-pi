# SDCC / ucsim_z80 build products (regenerated by `make`)
*.ihx
*.rel
*.lst
*.map
*.sym
*.noi
*.lk
*.asm
*.bin
*.o

# shared-memory host binaries (compiled by `make`)
parallel/shared-memory/t63
parallel/shared-memory/irq_host

# router scratch (ucsim per-node config / fifos)
parallel/distributed-memory/n[0-9].cfg
parallel/distributed-memory/n[0-9].in
parallel/distributed-memory/n[0-9].out

# python
__pycache__/
*.pyc

# NOTE: graphs/*.png and graphs/*.svg ARE committed on purpose so the
# figures are viewable without installing matplotlib. Regenerate with
# `cd graphs && make`.
